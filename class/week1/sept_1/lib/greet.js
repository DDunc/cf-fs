'use strict';

//reads right to left, three pointers and one expression.
var greet = module.exports = exports = function(name){
	debugger; //remove these before shipping
	return 'hello ' + name;
}

console.log(greet('some guy'));